let version = "0.0.1",
    lynx = = function( selector, context ) {
		return new lynx.fn.init( selector, context );
	},

class Lynx (
    constructor(){
        this.version;
    }
    get (){

    }
)